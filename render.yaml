services:
  - type: web
    name: meditalk-backend
    env: python
    plan: free
    buildCommand: pip install -r backend/requirements.txt
    startCommand: bash backend/start.sh
    envVars:
      - key: TRANSFORMERS_CACHE
        value: /tmp/cache
    autoDeploy: true
